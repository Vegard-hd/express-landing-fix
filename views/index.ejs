<!DOCTYPE html>
<html lang="en" data-theme="nord">
  <%- include('./partials/head.ejs') %>
  <body >
    <main class="text-center fs-5 min-h-screen">
      <section>
        <h1
          class="text-center text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-blue-500 text-4xl md:text-5xl lg:text-7xl pt-10 pb-6 z-50"
        >
          VegardHaglund.dev
        </h1>
      </section>

      <section>
        <%- include('./partials/hero.ejs') %>
      </section>
      <section>
        <%- include('./partials/siteTracker.ejs', {stats: stats, formatSeconds: formatSeconds}) %>
      </section>
  </main>
    <%- include('./partials/footer.ejs') %>
  </body>
</html>


<script>
  window.addEventListener('scroll', () => {
    const scrollPosition = window.scrollY;
    console.log(`Current scroll position: ${scrollPosition}`);
    
    // Example: Change header opacity based on scroll position
    const header = document.querySelector('h1');
    if (header) {
      header.style.opacity = Math.max(1 - scrollPosition / 300, 0);
    }
  });
</script>